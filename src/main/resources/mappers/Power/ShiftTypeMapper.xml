<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gson.his.model.mappers.Power.ShiftTypeMapper">
    <resultMap id="Type" type="cn.gson.his.model.pojos.Power.ShiftType">
        <id property="typeId" column="TYPE_ID"/>
        <result property="typeName" column="TYPE_NAME"/>
        <result property="typeLength" column="TYPE_LENGTH"/>
    </resultMap>
    <select id="allType" resultMap="Type">
        SELECT * FROM SHIFT_TYPE
    </select>
</mapper>